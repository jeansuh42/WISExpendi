<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.WISExpendi.wiselab.commonMapper">

    <insert id="insertRecipt" parameterType="com.wiselab.WISExpendi.DTO.ExpenditureCell">
        INSERT INTO RECIPT
        (
            prchDate,
            abstr,
            placePymnt,
            bdgtAmnt,
            pymntAmnt,
            rmks,
            regID
        ) VALUES (
            #{date}
          , #{abs}
          , #{expendDepartment}
          , #{budgetAmt}
          , #{paymentAmt}
          , #{etc}
          , #{regID}
        )
    </insert>

</mapper>